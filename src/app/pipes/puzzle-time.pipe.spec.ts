import { PuzzleTimePipe } from './puzzle-time.pipe';

describe('PuzzleTimePipe', () => {
  it('create an instance', () => {
    const pipe = new PuzzleTimePipe();
    expect(pipe).toBeTruthy();
  });
});
